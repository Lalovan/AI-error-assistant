# AI-error-assistant
A VS Code extension that sends my code + error messages to a small backend (running in Docker), which calls Groq’s LLM API and returns an explanation and guidance (no code solution!).
